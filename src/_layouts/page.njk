---
layout: default
---

<div class="page">
  <h1 class="page-title">{{ i18n[lang].titles[title] | default(title) }}</h1>
  <br>
  {{ content | safe }}
</div>
{#- Renders only under an issue; a no-op for site-level and info pages. -#}
{%- include "footer-nav.njk" -%}
